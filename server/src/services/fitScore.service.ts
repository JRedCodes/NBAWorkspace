// Implemented in Phase 2 — after player_computed_metrics are populated
export const fitScoreService = {
  async getPlayerTeamFit(
    _playerId: string,
    _teamId: string,
  ): Promise<null> {
    return null
  },
}
